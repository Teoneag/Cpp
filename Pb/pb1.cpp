#include <iostream>
using namespace std;

int main() {
    char c1[50], c2[50];
    cin.getline(c1, 50);
    cin.getline(c2, 50);
    int a[51] = {0};
    int carry = 0;
    int n1 = strlen(c1) - 1;
    int n2 = strlen(c2) - 1;
    while (n1 >= 0 && n2 >= 0) {
        a[]
    }
}